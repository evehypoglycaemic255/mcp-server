# Plugin: github_integration

Plugin tích hợp Git local và GitHub workflow ở mức cơ bản.

## Tools
- `git_local_status`
- `git_local_checkout`
- `git_local_commit`
- `github_remote_create_pr`

## Ghi chú
- Cần workspace root có `.git`.
- Trong Docker cần image có `git` và mount workspace phù hợp.
